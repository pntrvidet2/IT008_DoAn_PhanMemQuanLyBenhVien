<Window x:Class="DoAn.Windows.AddBillWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm mới hóa đơn" Height="580" Width="500"
        WindowStyle="None" ResizeMode="NoResize" Background="Transparent"
        AllowsTransparency="True" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ModernInput" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}" 
                                BorderBrush="#E0E0E0" BorderThickness="1">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" Margin="2,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2E78A6"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="#F5F7F9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="20" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="0" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="LẬP HÓA ĐƠN MỚI" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                <Rectangle HorizontalAlignment="Left" Height="3" Width="40" Fill="#2E78A6" Margin="0,5,0,0"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Mã hóa đơn (Tự động)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtBillId" Style="{StaticResource ModernInput}" IsReadOnly="True" Margin="0,0,0,15" Foreground="#2E78A6" FontWeight="Bold"/>

                    <TextBlock Text="Chọn bệnh nhân" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="cboPatient" Height="38" Margin="0,0,0,15" DisplayMemberPath="DisplayText" SelectedValuePath="Patient_ID"/>

                    <TextBlock Text="Nhân viên lập" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtEmpId" Style="{StaticResource ModernInput}" IsReadOnly="True" Margin="0,0,0,15"/>

                    <TextBlock Text="Tổng tiền (VNĐ)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtTotal" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="PT Thanh toán" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="cboPaymentMethod" Height="38">
                                <ComboBoxItem Content="Tiền mặt" IsSelected="True"/>
                                <ComboBoxItem Content="Chuyển khoản"/>
                                <ComboBoxItem Content="Thẻ tín dụng"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Trạng thái" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="cboStatus" Height="38">
                                <ComboBoxItem Content="Paid" IsSelected="True"/>
                                <ComboBoxItem Content="Unpaid"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" BorderBrush="#F0F0F0" BorderThickness="0,1,0,0" Padding="0,20,0,0" Margin="0,15,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="HỦY" Width="90" 
                            Background="#F8F9FA" Foreground="#666" BorderBrush="#DDD" BorderThickness="1"
                            Style="{StaticResource RoundedButton}" Click="BtnCancel_Click" Margin="0,0,12,0"/>

                    <Button Content="LƯU HÓA ĐƠN" Width="140" 
                            Background="#2C3E50" Foreground="White" BorderThickness="0"
                            Style="{StaticResource RoundedButton}" Click="BtnSave_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>