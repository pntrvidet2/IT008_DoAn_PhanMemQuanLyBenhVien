<Window x:Class="DoAn.Windows.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm nhân viên" Height="620" Width="850"
        WindowStyle="None" ResizeMode="NoResize" Background="Transparent"
        AllowsTransparency="True" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <Style x:Key="ModernInput" TargetType="Control">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border x:Name="border" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="#E0E0E0" 
                                BorderThickness="1">
                            <ContentControl Margin="10,0" VerticalAlignment="Center">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" />
                            </ContentControl>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2E78A6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource ModernInput}"/>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#357ABD"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="15" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="0" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel HorizontalAlignment="Left">
                    <TextBlock Text="THÊM MỚI NHÂN VIÊN" FontSize="20" FontWeight="Bold" Foreground="#1A4D80"/>
                    <Rectangle HorizontalAlignment="Left" Height="3" Width="50" Fill="#357ABD" Margin="0,5,0,0"/>
                </StackPanel>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Foreground="#999" Background="Transparent" BorderThickness="0" 
                        FontSize="20" Click="Cancel_Click" Cursor="Hand"/>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Họ và tên" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtName" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <TextBlock Text="Số CCCD/Định danh" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtCID" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <TextBlock Text="Giới tính" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="cbGender" SelectedIndex="0" Margin="0,0,0,15">
                        <ComboBoxItem Content="Male"/>
                        <ComboBoxItem Content="Female"/>
                    </ComboBox>

                    <TextBlock Text="Số điện thoại" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtPhone" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <TextBlock Text="Địa chỉ" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtAddress" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <TextBlock Text="Email" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtEmail" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Ngày sinh" Style="{StaticResource FieldLabel}"/>
                    <DatePicker x:Name="dpDOB" Height="38" Margin="0,0,0,15"/>

                    <TextBlock Text="Ngày vào làm" Style="{StaticResource FieldLabel}"/>
                    <DatePicker x:Name="dpStartDate" Height="38" Margin="0,0,0,15"/>

                    <TextBlock Text="Mức lương (VNĐ)" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtSalary" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <TextBlock Text="Khoa (Department)" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="cbDept" DisplayMemberPath="Depart_Name" SelectedValuePath="Depart_ID" Margin="0,0,0,15"/>

                    <TextBlock Text="Chức vụ (Position)" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="cbType" Margin="0,0,0,15">
                        <ComboBoxItem Content="Nhân viên nhập liệu" Tag="EC003"/>
                        <ComboBoxItem Content="Bác sĩ" Tag="DC002"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" BorderBrush="#F0F0F0" BorderThickness="0,1,0,0" Padding="0,20,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="THOÁT" Width="100" Height="40" Margin="0,0,12,0" 
                            Style="{StaticResource SecondaryButton}" Click="Cancel_Click"/>
                    <Button Content="TẠO HỒ SƠ" Width="150" Height="40" 
                            Style="{StaticResource PrimaryButton}" Click="Save_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>