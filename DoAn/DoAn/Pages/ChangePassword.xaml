<Window x:Class="DoAn.Pages.ChangePassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đổi mật khẩu"
        Height="420" Width="520"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <SolidColorBrush x:Key="Bg" Color="#F5F7FB"/>
        <SolidColorBrush x:Key="Card" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Primary" Color="#2563EB"/>
        <SolidColorBrush x:Key="Text" Color="#111827"/>
        <SolidColorBrush x:Key="SubText" Color="#6B7280"/>

        <DropShadowEffect x:Key="Shadow" BlurRadius="18" ShadowDepth="6" Opacity="0.16"/>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="12" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SoftBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="#F3F4F6"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource Bg}" CornerRadius="16" Padding="14">
        <Grid>
            <!-- Card -->
            <Border Background="{StaticResource Card}" CornerRadius="16" Padding="22" Effect="{StaticResource Shadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="44" Height="44" CornerRadius="22" Background="#EEF2FF">
                                <TextBlock Text="🔒" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Đổi mật khẩu" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Text}"/>
                                <TextBlock Text="Yêu cầu đổi mật khẩu lần đầu để bảo mật tài khoản"
                                           FontSize="12" Foreground="{StaticResource SubText}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- New pass -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,12">
                        <TextBlock Text="Mật khẩu mới" Foreground="{StaticResource SubText}" FontSize="14" Margin="2,0,0,6"/>
                        <Border CornerRadius="12" Background="#F3F4F6" Padding="12,0">
                            <PasswordBox x:Name="txtNewPass"/>
                        </Border>
                    </StackPanel>

                    <!-- Confirm pass -->
                    <StackPanel Grid.Row="2" Margin="0,0,0,14">
                        <TextBlock Text="Xác nhận mật khẩu" Foreground="{StaticResource SubText}" FontSize="14" Margin="2,0,0,6"/>
                        <Border CornerRadius="12" Background="#F3F4F6" Padding="12,0">
                            <PasswordBox x:Name="txtConfirmPass"/>
                        </Border>
                    </StackPanel>

                    <!-- Hint -->
                    <TextBlock Grid.Row="3"
                               Text="Gợi ý: Mật khẩu tối thiểu 6 ký tự."
                               Foreground="{StaticResource SubText}"
                               FontSize="12"
                               Margin="0,0,0,16"/>

                    <!-- Buttons -->
                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="Hủy" Style="{StaticResource SoftBtn}" Click="btnCancel_Click"/>
                        <Button Grid.Column="2" Content="Xác nhận" Style="{StaticResource PrimaryBtn}" Click="btnConfirm_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
