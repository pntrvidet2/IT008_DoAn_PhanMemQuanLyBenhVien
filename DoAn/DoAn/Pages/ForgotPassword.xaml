<Window x:Class="DoAn.Pages.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quên mật khẩu" Height="500" Width="400" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">
    <Border Background="White" CornerRadius="25" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.15" Direction="-90"/>
        </Border.Effect>
        <StackPanel Margin="30">
            <TextBlock Text="QUÊN MẬT KHẨU" FontSize="18" FontWeight="Bold" Foreground="#2E78A6" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Email đăng ký" Foreground="#64748B" Margin="0,10,0,5"/>
            <Grid>
                <TextBox x:Name="txtUsernameEmail" Height="40" Padding="10,0" VerticalContentAlignment="Center">
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Border CornerRadius="10" BorderBrush="#CBD5E1" BorderThickness="1">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
                <Button x:Name="btnSendOTP" Content="Gửi mã" HorizontalAlignment="Right" Width="80" Margin="5" 
                        Background="#2E78A6" Foreground="White" Click="btnSendOTP_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <TextBlock Text="Nhập mã OTP" Foreground="#64748B" Margin="0,20,0,5"/>
            <TextBox x:Name="txtOTP" Height="40" Padding="10,0" VerticalContentAlignment="Center" FontSize="16" FontWeight="Bold">
                <TextBox.Template>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="10" BorderBrush="#CBD5E1" BorderThickness="1">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </TextBox.Template>
            </TextBox>

            <TextBlock x:Name="txtTimer" Text="" Foreground="#94A3B8" FontSize="12" Margin="5,5,0,0" FontStyle="Italic"/>
            <TextBlock x:Name="txtError" Text="" Foreground="#EF4444" FontSize="12" HorizontalAlignment="Center" Margin="0,10,0,0" FontWeight="SemiBold"/>

            <Button x:Name="btnVerify" Content="XÁC MINH" Height="45" Margin="0,20,0,0" Background="#2E78A6" Foreground="White" FontWeight="Bold" Click="btnVerify_Click" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button x:Name="btnResend" Content="Gửi lại mã" Visibility="Collapsed" Margin="0,10,0,0" Background="Transparent" Foreground="#2E78A6" Click="btnResend_Click" BorderThickness="0"/>
            <Button Content="Quay lại" Margin="0,10,0,0" Background="Transparent" Foreground="#94A3B8" Click="btnCancel_Click" BorderThickness="0"/>
        </StackPanel>
    </Border>
</Window>