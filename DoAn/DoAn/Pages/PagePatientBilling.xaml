<Page x:Class="DoAn.Pages.PagePatientBilling"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Thanh toán hóa đơn">

    <Grid Background="White" Width="639">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="DANH SÁCH HÓA ĐƠN VIỆN PHÍ" FontSize="20" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,25"/>

        <DataGrid x:Name="dgBills" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                  RowHeight="55" Background="Transparent" BorderThickness="0" 
                  GridLinesVisibility="None" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="MÃ HĐ" Binding="{Binding Bill_ID}" Width="Auto" MinWidth="100"/>

                <DataGridTextColumn Header="NGÀY LẬP" Binding="{Binding Date, StringFormat='dd/MM/yyyy'}" Width="150"/>

                <DataGridTextColumn Header="SỐ TIỀN" Binding="{Binding Total, StringFormat='N0'}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Margin" Value="0,0,20,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="TRẠNG THÁI" Binding="{Binding Payment_Status}" Width="150"/>

                <DataGridTemplateColumn Header="THAO TÁC" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Thanh Toán QR" Click="btnPay_Click" Tag="{Binding Bill_ID}" HorizontalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#2E78A6"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Padding" Value="15,8"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Payment_Status}" Value="Paid">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>