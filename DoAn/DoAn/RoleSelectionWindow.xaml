<Window x:Class="DoAn.RoleSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CHỌN VAI TRÒ ĐĂNG NHẬP"
        Height="420" Width="720"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <SolidColorBrush x:Key="Primary" Color="#2E78A6"/>

        <Style x:Key="TopBtn" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoleCard" TargetType="RadioButton">
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd"
                                Background="White"
                                BorderBrush="#E5E7EB"
                                BorderThickness="1.5"
                                CornerRadius="18"
                                Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Width="40" Height="40"
                                        CornerRadius="12"
                                        Background="#F0F7FF"
                                        VerticalAlignment="Center">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{TemplateBinding Tag}"
                                               FontWeight="Bold"
                                               FontSize="15"
                                               Foreground="#1E293B"/>
                                    <TextBlock Text="{TemplateBinding ToolTip}"
                                               Foreground="#64748B"
                                               FontSize="11"
                                               Margin="0,2,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#93C5FD"/>
                                <Setter TargetName="Bd" Property="Background" Value="#FBFDFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#2E78A6"/>
                                <Setter TargetName="Bd" Property="Background" Value="#F0F9FF"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="22">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SoftBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="22" BorderBrush="#E2E8F0" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="25"
            Background="White"
            BorderBrush="{StaticResource Primary}"
            BorderThickness="1.5"
            ClipToBounds="True">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource Primary}"
                    MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
                <Grid Margin="20,0">
                    <TextBlock Text="VAI TRÒ HỆ THỐNG"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="14"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"/>

                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Top">
                        <Button Content="—" 
            Style="{StaticResource TopBtn}" 
            FontSize="14" 
            Click="BtnMinimize_Click"/>

                        <Button x:Name="BtnMax" 
            Content="☐" 
            Style="{StaticResource TopBtn}" 
            FontSize="16" 
            Click="BtnMaximize_Click"/>

                        <Button Content="✕" 
            FontSize="14" 
            Click="BtnClose_Click">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource TopBtn}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E81123"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="25,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <RadioButton x:Name="rbDataEntry" GroupName="role" Style="{StaticResource RoleCard}" Tag="Nhân viên nhập liệu" ToolTip="Dữ liệu thuốc, bệnh nhân, hóa đơn">
                        <TextBlock Text="⌨️" FontSize="20"/>
                    </RadioButton>
                    <RadioButton x:Name="rbDoctor" GroupName="role" Style="{StaticResource RoleCard}" Tag="Bác sĩ" ToolTip="Khám bệnh, chỉ định, hồ sơ">
                        <TextBlock Text="👨‍⚕️" FontSize="20"/>
                    </RadioButton>
                    <RadioButton x:Name="rbPatient" GroupName="role" Style="{StaticResource RoleCard}" Tag="Bệnh nhân" ToolTip="Hồ sơ cá nhân, hóa đơn">
                        <TextBlock Text="👤" FontSize="20"/>
                    </RadioButton>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <RadioButton x:Name="rbHR" GroupName="role" Style="{StaticResource RoleCard}" Tag="Quản lý nhân sự" ToolTip="Nhân viên, khoa, bảng lương">
                        <TextBlock Text="🗂️" FontSize="20"/>
                    </RadioButton>
                    <RadioButton x:Name="rbAdmin" GroupName="role" Style="{StaticResource RoleCard}" Tag="Admin" ToolTip="Quản trị toàn bộ hệ thống">
                        <TextBlock Text="🛡️" FontSize="20"/>
                    </RadioButton>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#F1F5F9" BorderThickness="0,1,0,0">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnCancel" Content="Hủy bỏ" Width="100" Margin="0,0,12,0" Style="{StaticResource SoftBtn}" Click="BtnCancel_Click"/>
                        <Button x:Name="btnContinue" Content="Tiếp tục đăng nhập" Width="180" Style="{StaticResource PrimaryBtn}" Click="BtnContinue_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>