<Window x:Class="DoAn.Windows.UpdateDrugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CẬP NHẬT THÔNG TIN THUỐC" Height="450" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F3F4F6">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="Margin" Value="5,0,0,5"/>
        </Style>

        <Style x:Key="InputContainer" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="InputIcon" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Border Padding="25">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="#2E78A6" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="CẬP NHẬT THUỐC" FontSize="20" FontWeight="ExtraBold" Foreground="#1F2937" VerticalAlignment="Center"/>
                </StackPanel>
                <Border Height="4" Width="40" Background="#2E78A6" HorizontalAlignment="Left" Margin="34,5,0,0" CornerRadius="2"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock Text="Mã số thuốc" Style="{StaticResource FieldLabel}"/>
                <Border Style="{StaticResource InputContainer}" Background="#F9FAFB">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" Style="{StaticResource InputIcon}"/>
                        <TextBox x:Name="txtDrugId" IsReadOnly="True" Width="320"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Giá bán mới (VNĐ)" Style="{StaticResource FieldLabel}"/>
                <Border Style="{StaticResource InputContainer}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" Style="{StaticResource InputIcon}"/>
                        <TextBox x:Name="txtPrice" Width="320"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Số lượng tồn kho" Style="{StaticResource FieldLabel}"/>
                <Border Style="{StaticResource InputContainer}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" Style="{StaticResource InputIcon}"/>
                        <TextBox x:Name="txtQty" Width="320"/>
                    </StackPanel>
                </Border>

                <TextBlock x:Name="txtError" Foreground="#EF4444" FontSize="12" Margin="5,0,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Hủy bỏ" Width="100" Height="40" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Foreground="#6B7280" Click="Cancel_Click" Margin="0,0,12,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="Lưu thay đổi" Width="140" Height="40" Background="#2E78A6" Foreground="White" BorderThickness="0" FontWeight="Bold" Click="Save_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>