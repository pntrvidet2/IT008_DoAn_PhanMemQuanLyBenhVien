<Window x:Class="DoAn.Windows.UpdateEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CẬP NHẬT NHÂN VIÊN" Height="500" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2E78A6"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White" BorderBrush="#2E78A6" BorderThickness="2">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="CẬP NHẬT THÔNG TIN" FontSize="20" FontWeight="Bold" 
                       Foreground="#2E78A6" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Mã Nhân Viên" Foreground="#888" Margin="5,0"/>
                    <TextBox x:Name="txtID" Style="{StaticResource ModernTextBox}" IsEnabled="False" Background="#F8FAFC"/>

                    <TextBlock Text="Họ và Tên" FontWeight="SemiBold" Margin="5,0"/>
                    <TextBox x:Name="txtName" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Text="Khoa / Phòng Ban" FontWeight="SemiBold" Margin="5,0"/>
                    <ComboBox x:Name="cbDepartment" Height="35" Margin="0,5,0,15" 
                              DisplayMemberPath="Dept_Name" 
                              SelectedValuePath="Dept_ID"
                              Padding="10,0" VerticalContentAlignment="Center">
                        <ComboBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </ComboBox.Resources>
                    </ComboBox>

                    <TextBlock Text="Số Điện Thoại" FontWeight="SemiBold" Margin="5,0"/>
                    <TextBox x:Name="txtPhone" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Text="Địa Chỉ" FontWeight="SemiBold" Margin="5,0"/>
                    <TextBox x:Name="txtAddress" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Hủy" Width="90" Height="35" Margin="0,0,12,0" 
                        Background="#CBD5E1" Click="Cancel_Click" Cursor="Hand" BorderThickness="0"/>

                <Button Content="Lưu Thay Đổi" Width="120" Height="35" 
                        Background="#2E78A6" Foreground="White" Click="Save_Click" Cursor="Hand" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>