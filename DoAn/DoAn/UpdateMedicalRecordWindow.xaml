<Window x:Class="DoAn.Windows.UpdateMedicalRecordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cập nhật bệnh án" Height="600" Width="500"
        WindowStyle="None" ResizeMode="NoResize" Background="Transparent"
        AllowsTransparency="True" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ModernInput" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}" 
                                BorderBrush="#E0E0E0" BorderThickness="1">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" Margin="2,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2E78A6"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="#F5F7F9"/>
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="20" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="0" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="CẬP NHẬT HỒ SƠ BỆNH ÁN" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                <Rectangle HorizontalAlignment="Left" Height="3" Width="40" Fill="#2E78A6" Margin="0,5,0,0"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Mã bệnh án" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="txtRecordId" Style="{StaticResource ModernInput}" Height="38" IsReadOnly="True" Margin="0,0,0,15"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Mã bệnh nhân" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="txtPatientId" Style="{StaticResource ModernInput}" Height="38" IsReadOnly="True" Margin="0,0,0,15"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Triệu chứng" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtSymptoms" Style="{StaticResource ModernInput}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15"/>

                    <TextBlock Text="Chẩn đoán" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtDiagnosis" Style="{StaticResource ModernInput}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15"/>

                    <TextBlock Text="Cách điều trị" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtTreatment" Style="{StaticResource ModernInput}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15"/>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" BorderBrush="#F0F0F0" BorderThickness="0,1,0,0" Padding="0,20,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="HỦY" Width="90" Background="#F8F9FA" Foreground="#666" 
                            Style="{StaticResource RoundedButton}" Click="BtnCancel_Click" Margin="0,0,12,0"/>
                    <Button Content="LƯU THAY ĐỔI" Width="140" Background="#2E78A6" Foreground="White" 
                            Style="{StaticResource RoundedButton}" Click="BtnSave_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>